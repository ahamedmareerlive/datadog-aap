#################################################
# ✅ GLOBAL VARS
#################################################

- name: GLOBAL VARS HOLDER
  hosts: localhost
  gather_facts: no
  vars:
    servicenow_instance: "nowlearning-nlinst04302549-63gqx-0001.lab.service-now.com"
    search_text: "Datadog"
  tasks: []


#################################################
# ✅ PLAY 1: GET SR + SET IN PROGRESS
#################################################

- name: Get SR and prepare execution
  hosts: localhost
  gather_facts: no
  become: no

  vars:
    servicenow_instance: "nowlearning-nlinst04302549-63gqx-0001.lab.service-now.com"

  tasks:

    ############################
    # ✅ GET LATEST SR
    ############################

    - name: Get latest SR
      uri:
        url: "https://{{ servicenow_instance }}/api/now/table/sc_req_item?sysparm_query=short_descriptionLIKEDatadog^active=true^state=1&sysparm_order_by_desc=sys_created_on&sysparm_limit=1"
        method: GET
        user: "{{ snow_user }}"
        password: "{{ snow_password }}"
        force_basic_auth: yes
        return_content: yes
      register: sn

    - name: Extract IDs
      set_fact:
        request_item_sys_id: "{{ sn.json.result[0].sys_id }}"
        request_sys_id: "{{ sn.json.result[0].request.value }}"

    - debug:
        msg: "✅ Selected Ticket = {{ sn.json.result[0].number }}"

    ############################
    # ✅ SET RITM → IN PROGRESS
    ############################

    - name: Set RITM to In Progress
      uri:
        url: "https://{{ servicenow_instance }}/api/now/table/sc_req_item/{{ request_item_sys_id }}"
        method: PATCH
        user: "{{ snow_user }}"
        password: "{{ snow_password }}"
        body_format: json
        body:
          state: "2"

    ############################
    # ✅ GET VARIABLES
    ############################

    - name: Get variables
      uri:
        url: "https://{{ servicenow_instance }}/api/now/table/sc_item_option_mtom?sysparm_query=request={{ request_item_sys_id }}"
        method: GET
        user: "{{ snow_user }}"
        password: "{{ snow_password }}"
      register: mtom

    ############################
    # ✅ PICK LATEST VARIABLE
    ############################

    - name: Sort variables
      set_fact:
        sorted_mtom: "{{ mtom.json.result | sort(attribute='sys_created_on') }}"

    - name: Get latest option id
      set_fact:
        option_sys_id: "{{ sorted_mtom[-1].sc_item_option.value }}"

    ############################
    # ✅ GET HOST
    ############################

    - name: Get hostname value
      uri:
        url: "https://{{ servicenow_instance }}/api/now/table/sc_item_option/{{ option_sys_id }}"
        method: GET
        user: "{{ snow_user }}"
        password: "{{ snow_password }}"
      register: opt

    - name: Set host
      set_fact:
        target_host: "{{ opt.json.result.value }}"

    - debug:
        msg: "✅ TARGET HOST = {{ target_host }}"

    ############################
    # ✅ VALIDATE HOST
    ############################

    - name: Validate host
      fail:
        msg: "❌ Invalid host {{ target_host }}"
      when: target_host not in groups['all']

    ############################
    # ✅ ADD HOST
    ############################

    - name: Add dynamic host
      add_host:
        name: "{{ target_host }}"
        groups: dynamic_targets
        ansible_host: "{{ hostvars[target_host].ansible_host }}"


#################################################
# ✅ PLAY 2: INSTALL DATADOG
#################################################

- name: Install Datadog
  hosts: dynamic_targets
  gather_facts: yes
  become: yes

  tasks:

    - name: Confirm host
      debug:
        msg: "🚀 Running on {{ inventory_hostname }}"

    - block:

        - name: Check installation
          stat:
            path: /etc/datadog-agent/datadog.yaml
          register: dd

        - name: Download installer
          get_url:
            url: https://install.datadoghq.com/scripts/install_script_agent7.sh
            dest: /tmp/datadog_install.sh
            mode: '0755'
          when: not dd.stat.exists

        - name: Install Datadog
          shell: |
            DD_API_KEY={{ datadog_api_key }} \
            DD_SITE="us5.datadoghq.com" \
            bash /tmp/datadog_install.sh
          when: not dd.stat.exists

        - name: Start service
          service:
            name: datadog-agent
            state: started
            enabled: yes

        - name: Mark success
          set_fact:
            install_success: true

      rescue:

        - name: Mark failure
          set_fact:
            install_success: false


#################################################
# ✅ PLAY 3: UPDATE SERVICENOW STATUS
#################################################

- name: Update ServiceNow
  hosts: localhost
  gather_facts: no
  become: no

  vars:
    servicenow_instance: "nowlearning-nlinst04302549-63gqx-0001.lab.service-now.com"

  tasks:

    #################################################
    # ✅ SUCCESS CASE
    #################################################

    - name: Close RITM
      uri:
        url: "https://{{ servicenow_instance }}/api/now/table/sc_req_item/{{ request_item_sys_id }}"
        method: PATCH
        user: "{{ snow_user }}"
        password: "{{ snow_password }}"
        body_format: json
        body:
          state: "3"
      when: hostvars['dynamic_targets'][0].install_success | default(false)

    - name: Close REQUEST
      uri:
        url: "https://{{ servicenow_instance }}/api/now/table/sc_request/{{ request_sys_id }}"
        method: PATCH
        user: "{{ snow_user }}"
        password: "{{ snow_password }}"
        body_format: json
        body:
          state: "3"
      when: hostvars['dynamic_targets'][0].install_success | default(false)

    #################################################
    # ❌ FAILURE CASE
    #################################################

    - name: Set RITM to Pending
      uri:
        url: "https://{{ servicenow_instance }}/api/now/table/sc_req_item/{{ request_item_sys_id }}"
        method: PATCH
        user: "{{ snow_user }}"
        password: "{{ snow_password }}"
        body_format: json
        body:
          state: "-5"
      when: not (hostvars['dynamic_targets'][0].install_success | default(false))
