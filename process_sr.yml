#################################################
# ✅ PLAY 1: GET HOST FROM SERVICENOW
#################################################

- name: Get target host from ServiceNow
  hosts: localhost
  gather_facts: no
  become: no

  vars:
    servicenow_instance: "nowlearning-nlinst04302549-63gqx-0001.lab.service-now.com"
    search_text: "Datadog"

  tasks:

    #################################################
    # ✅ GET LATEST SR
    #################################################

    - name: Get latest SR
      uri:
        url: "https://{{ servicenow_instance }}/api/now/table/sc_req_item?sysparm_query=short_descriptionLIKE{{ search_text }}^ORDERBYDESCsys_created_on&sysparm_limit=1"
        method: GET
        user: "{{ snow_user }}"
        password: "{{ snow_password }}"
        force_basic_auth: yes
        return_content: yes
      register: sn

    - name: Debug SR
      debug:
        msg: "SR={{ sn.json.result[0].number }} | {{ sn.json.result[0].short_description }}"

    #################################################
    # ✅ EXTRACT SYS_ID
    #################################################

    - set_fact:
        request_sys_id: "{{ sn.json.result[0].sys_id }}"

    #################################################
    # ✅ GET HOSTNAME VARIABLE
    #################################################

    - name: Get mtom
      uri:
        url: "https://{{ servicenow_instance }}/api/now/table/sc_item_option_mtom?sysparm_query=request={{ request_sys_id }}"
        method: GET
        user: "{{ snow_user }}"
        password: "{{ snow_password }}"
      register: mtom

    - set_fact:
        option_sys_id: "{{ mtom.json.result[0].sc_item_option.value }}"

    - name: Get hostname value
      uri:
        url: "https://{{ servicenow_instance }}/api/now/table/sc_item_option/{{ option_sys_id }}"
        method: GET
        user: "{{ snow_user }}"
        password: "{{ snow_password }}"
      register: opt

    #################################################
    # ✅ SET TARGET HOST
    #################################################

    - set_fact:
        target_host: "{{ opt.json.result.value }}"

    #################################################
    # ✅ DEBUG HOST
    #################################################

    - debug:
        msg: "TARGET HOST from ServiceNow = {{ target_host }}"

    #################################################
    # ✅ VALIDATION (VERY IMPORTANT 🔥)
    #################################################

    - name: Fail if invalid host
      fail:
        msg: "❌ Invalid hostname from ServiceNow → {{ target_host }}"
      when: target_host not in groups['all']

    #################################################
    # ✅ ADD HOST (CORRECT WAY)
    #################################################

    - name: Add host dynamically
      add_host:
        name: "{{ target_host }}"
        groups: dynamic_targets
        ansible_host: "{{ hostvars[target_host].ansible_host }}"


#################################################
# ✅ PLAY 2: INSTALL DATADOG
#################################################

- name: Install Datadog Agent
  hosts: dynamic_targets
  gather_facts: yes
  become: yes
  become_method: sudo

  tasks:

    #################################################
    # ✅ CONFIRM HOST
    #################################################

    - name: Confirm execution host
      debug:
        msg: "Running on {{ inventory_hostname }}"

    #################################################
    # ✅ CHECK INSTALLATION
    #################################################

    - name: Check Datadog installation
      stat:
        path: /etc/datadog-agent/datadog.yaml
      register: dd

    - debug:
        msg: "Already installed = {{ dd.stat.exists }}"

    #################################################
    # ✅ CLEAN OLD CONFIG
    #################################################

    - name: Remove Debian keyring
      file:
        path: /usr/share/keyrings/datadog-archive-keyring.gpg
        state: absent
      when:
        - ansible_os_family == "Debian"
        - not dd.stat.exists

    - name: Remove Debian repo
      file:
        path: /etc/apt/sources.list.d/datadog.list
        state: absent
      when:
        - ansible_os_family == "Debian"
        - not dd.stat.exists

    - name: Remove RHEL repo
      file:
        path: /etc/yum.repos.d/datadog.repo
        state: absent
      when:
        - ansible_os_family == "RedHat"
        - not dd.stat.exists

    #################################################
    # ✅ DOWNLOAD INSTALLER
    #################################################

    - name: Download installer
      get_url:
        url: https://install.datadoghq.com/scripts/install_script_agent7.sh
        dest: /tmp/datadog_install.sh
        mode: '0755'
        force: yes
      when: not dd.stat.exists

    #################################################
    # ✅ INSTALL DATADOG
    #################################################

    - name: Install Datadog
      shell: |
        DD_API_KEY={{ datadog_api_key }} \
        DD_SITE="us5.datadoghq.com" \
        DD_AGENT_MAJOR_VERSION=7 \
        DD_APM_INSTRUMENTATION_ENABLED=host \
        DD_DATA_STREAMS_ENABLED=true \
        DD_PROFILING_ENABLED=auto \
        DD_LOGS_CONFIG_PROCESS_COLLECT_ALL=true \
        bash /tmp/datadog_install.sh
      args:
        executable: /bin/bash
      when: not dd.stat.exists

    #################################################
    # ✅ START SERVICE
    #################################################

    - name: Ensure Datadog running
      service:
        name: datadog-agent
        state: started
        enabled: yes

    #################################################
    # ✅ VERIFY
    #################################################

    - name: Check service
      shell: systemctl is-active datadog-agent
      register: dd_status

    - debug:
        msg: "Datadog service = {{ dd_status.stdout }}"

    #################################################
    # ✅ SUCCESS
    #################################################

    - debug:
        msg: "✅ Datadog installed successfully on {{ inventory_hostname }}"
